
// Change the target environment manually BEFORE moving to host-environment
class Environment {

    // Available environments: "LOCAL", "TESTING", "PRODUCTION"
    getActiveEnvironment() {
        return "LOCAL";
    }

}